import type { MetaDict } from "../en/meta";

/**
 * meta namespace — French.
 *
 * `brand`, `wordmark` and `title` are the brand name and stay untranslated.
 * Only `description` carries copy.
 */
export const meta: MetaDict = {
  brand: "Global Language Solutions",
  wordmark: "GLS",
  title: "Global Language Solutions",
  description:
    "Traduction certifiée et interprétation pour l’immigration, les études et l’entreprise. Chaque traduction certifiée est vérifiée sur le document source par un second linguiste.",
};
